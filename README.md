# Jurisdiction-data-pull
An application, that pulls data from two datasets related to Cities and Counties. The data set has populations from each jurisdiction and it also has geo codes that can be used for jSON mapping. 
